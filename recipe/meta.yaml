{% set name = "arraykit" %}
{% set version = "0.3.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/arraykit-{{ version }}.tar.gz
  sha256: 241e3093f285f8e6b94769f259f3704139bfc00ed966aa441d0c78d995a6c827

build:
  number: 0
  skip: true  # [py<37]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - numpy ==1.18.5  # [py==37]
    - numpy ==1.18.5  # [py==38]
    - numpy >=1.18.5  # [py>=39]
  run:
    - python
    - numpy ==1.18.5  # [py==37]
    - numpy ==1.18.5  # [py==38]
    - numpy >=1.18.5  # [py>=39]

test:
  imports:
    - arraykit
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/static-frame/arraykit
  summary: Array utilities for StaticFrame
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - flexatone
    - chaburkland
