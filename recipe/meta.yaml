{% set name = "arraykit" %}
{% set version = "0.3.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/arraykit-{{ version }}.tar.gz
  sha256: 65e6cbbbf71e4d6abaf0a15b31d0ea19b52a3d72a5c9539f384516610643a547

build:
  number: 0
  skip: true  # [py<37]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - numpy ==1.18.5  # [py==37]
    - numpy ==1.18.5  # [py==38]
    - numpy >=1.18.5  # [py>=39]
  run:
    - python
    - numpy ==1.18.5  # [py==37]
    - numpy ==1.18.5  # [py==38]
    - numpy >=1.18.5  # [py>=39]

test:
  imports:
    - arraykit
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/static-frame/arraykit
  summary: Array utilities for StaticFrame
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - flexatone
    - chaburkland
